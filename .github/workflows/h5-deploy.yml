name: H5 Deploy

# 小程序 H5 自动部署：构建静态产物 → rsync 到自有云主机 → Nginx 原子切换
# 对齐 admin-deploy.yml 模式
# 前置：服务器已由 ops/scripts/server-bootstrap.sh 初始化

on:
  push:
    branches: [main]
    paths:
      - 'h5/**'
      - '.github/workflows/h5-deploy.yml'
  workflow_dispatch:

concurrency:
  group: h5-deploy-prod
  cancel-in-progress: true

jobs:
  deploy:
    name: Build & Deploy to Cloud Server
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://wuxian-hotel.online/h5/

    defaults:
      run:
        working-directory: ./h5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: h5/package-lock.json

      - name: Install deps
        run: npm ci

      - name: Build (Vite)
        run: npm run build

      - name: Pack dist
        run: |
          cd dist
          tar -czf ../../h5-${{ github.sha }}.tar.gz .
          ls -lh ../../h5-${{ github.sha }}.tar.gz

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Known hosts
        working-directory: .
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Upload artifact
        working-directory: .
        run: |
          scp -P ${{ secrets.SSH_PORT || 22 }} \
              h5-${{ github.sha }}.tar.gz \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/tmp/

      - name: Remote deploy (atomic symlink switch)
        working-directory: .
        run: |
          ssh -p ${{ secrets.SSH_PORT || 22 }} \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
              "bash -s" << 'EOF'
          set -euo pipefail

          H5_ROOT="/opt/hotel/h5"
          RELEASES="$H5_ROOT/releases"
          CURRENT="$H5_ROOT/current"
          SHA="${{ github.sha }}"
          TARGET="$RELEASES/$SHA"

          # 1. 解压到新 release 目录
          mkdir -p "$TARGET"
          tar -xzf "/tmp/h5-$SHA.tar.gz" -C "$TARGET"

          # 2. 原子切换（Nginx root 指向 current，不需 reload）
          ln -sfn "$TARGET" "$CURRENT"

          # 3. 健康检查：H5 首页是否可访问
          sleep 1
          if ! curl -fsS "https://wuxian-hotel.online/h5/" -o /dev/null; then
            echo "⚠️  域名暂未通或首次部署，跳过健康检查"
          else
            echo "✅ H5 reachable"
          fi

          # 4. 保留最近 5 个 release
          cd "$RELEASES"
          ls -1dt */ | tail -n +6 | xargs -r rm -rf

          # 5. 清理 tarball
          rm -f "/tmp/h5-$SHA.tar.gz"
          EOF

      - name: Notify on failure
        if: failure()
        run: echo "::error::H5部署失败 Commit=${{ github.sha }}"
